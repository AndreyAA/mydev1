package dev.fillin;

import java.util.ArrayDeque;
import java.util.ArrayList;
import java.util.Queue;

public class FillIn {

    private final static int size = 100;
/*    public int[][] fillin(int[][] src, int x, int y, int edgeColor, int fillinColor) {
        Queue<Integer> queue = new ArrayDeque<>();
        queue.add(y*size+x);

        int xx, yy;
        while (!queue.isEmpty()) {
            int pos = queue.remove();
             yy = pos/size;
             xx= pos%size;
             if (src[yy][xx]==fillinColor) {
                 continue;
             } else {
                addNeighbor(xx, yy);
                src[yy][xx]=fillinColor;

             }

            }

        }*/

    private void addNeighbor(int xx, int yy) {

    }
}


/*    public static class Data {
        int
    }*/

